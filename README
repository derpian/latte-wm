
                                 -= latte-wm =-

This is the window manager for Latte (http://latte-desktop.org).

To build and install latte-wm, execute the following:
1) ./configure --prefix=/usr --sysconfdir=/etc --libexecdir=/usr/lib --localstatedir=/var --disable-static --enable-startup-notification --enable-randr --enable-compositor --enable-xsync --disable-debug
2) make
3) make install
